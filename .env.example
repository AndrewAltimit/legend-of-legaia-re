# UID/GID for the Ghidra container (used by docker/ghidra.Dockerfile).
# Defaults to 1000:1000 if unset; override here to match a non-default host user:
#   USER_ID=$(id -u)
#   GROUP_ID=$(id -g)
# Then `docker compose build ghidra` to rebuild with the new mapping.
USER_ID=1000
GROUP_ID=1000
