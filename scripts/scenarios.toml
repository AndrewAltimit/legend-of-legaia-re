# Unified save-state scenario manifest.
#
# Single source of truth for save-state labelled scenarios. Each
# [[scenarios]] block maps one investigation label to per-emulator
# save-state paths + mednafen-specific overlay/watchpoint fields used
# by the `mednafen-state` toolkit.
#
# Consumers:
#   - `mednafen-state` CLI + `scripts/mednafen/{auto-capture,state-walk}.sh`
#     (overlay-slice + watchpoint diff workflow, mednafen mc{0..9}).
#   - `scripts/pcsx-redux/run_probe.sh --scenario <label>` (resolves
#     `pcsx_redux_sstate` for the probe runner).
#   - `scripts/manage-states.py` (fingerprints / validates that each
#     emulator's save state hasn't drifted from the committed manifest).
#
# Per-scenario fields (all but slot+label+description are optional):
#   slot                       u8     mednafen .mc{0..9} slot index
#   label                      str    short investigation handle
#   description                str    human-readable purpose
#   topics                     list   free-text labels
#   diff_against               list   sister-slot indices for diff sweep
#   pcsx_redux_sstate          path   PCSX-Redux .sstate location ($HOME / ~ expanded at use time)
#   duckstation_sav            path   Duckstation .sav location
#   phase                      str    boot/title/menu/field/battle/world_map/cutscene
#   expected_game_mode         u8     _DAT_8007B83C value at scenario start
#   expected_active_scene      str    CDNAME label (e.g. "map01")
#   ram_fingerprint_sha256     hex    sha256 of first 64 KiB main RAM after settle
#   backup_fingerprint         hex    sha256 of an IMMUTABLE copy stashed in
#                                     saves/library/<emulator>/<fp>.<ext>. Live
#                                     emulator slots (SCUS94254.sstateN, mc{N})
#                                     get overwritten the next time you save, so
#                                     a scenario worth keeping must be backed up.
#                                     When set, manage-states.py + run_probe.sh
#                                     resolve THIS copy in preference to the live
#                                     path. The library is gitignored (Sony RAM);
#                                     this fingerprint is the committed pointer.
#   [scenarios.overlay_slice]  block  mednafen overlay-slice range
#   [[scenarios.watchpoints]]  block  mednafen diff watchpoint regions
#
# Adding a scenario:
#   1. Capture the state in an emulator (mednafen mc{N} / PCSX-Redux sstateN).
#   2. Back it up so it survives a slot overwrite, and record the pointer:
#        scripts/manage-states.py backup pcsx-redux <path> --label <label>
#      (copies to saves/library/, sets backup_fingerprint on the scenario).
#   3. Add/extend the [[scenarios]] block here with label / phase / scene.
#   4. (mednafen only) `manage-states.py fingerprint <label>` for the RAM
#      fingerprint; `manage-states.py library` lists what's backed up.
#   5. Commit (the manifest only - the save bytes stay local + gitignored).
#
# See `docs/tooling/mednafen-automation.md` for the diff-sweep workflow
# and `docs/tooling/pcsx-redux-automation.md` for the probe workflow.
#
# ## Current corpus rotation
#
# This rotation captures the **per-STR FMV trigger corpus**: nine save
# states taken right before each FMV begins playing, one per
# `_DAT_8007BA78` value (`fmv_id ∈ 0..=8`). Slot mapping (debug-menu
# driven, all from active scene `map01`):
#
#   mc2 → STR 0 (fmv_id 0)    mc3 → ROTATED (keikoku_chest_preload; was STR 1 / fmv_id 1)
#   mc4 → STR 2 (fmv_id 2)    mc5 → ROTATED (title_screen_new_game)
#   mc6 → ROTATED (v0.1 slice C+, dialogue-accept; was STR 4 / fmv_id 4)
#   mc7 → ROTATED (v0.1 slice C; was fmv_id 5)
#   mc8 → ROTATED (v0.1 slice E, post-battle; was fmv_id 6)
#   mc9 → STR 7 (fmv_id 7)
#   mc0 → ROTATED (v0.1 slice D; was fmv_id 8)
#
# (mc1 is unrelated to the FMV corpus in this rotation.)
#
# Existing slot-specific tests (encounter trigger, battle init, item-use,
# anim, level-up triplets, FMV-overlay residency) carry slot-fingerprint
# guards (`require_slot_scenes`) and skip-pass when the corpus has been
# rotated to a different shape. They remain in the test suite as
# documentation of the prior captures; the constants they reference are
# still authoritative for the engine layer.

[defaults]
# Filename pattern for the user's mednafen saves. {slot} is replaced with
# the slot index (0..9). The hash is mednafen's per-rom identifier - adjust
# if you're using a different US dump.
filename_pattern = "Legend of Legaia (USA).788de08f9c7e652c51d8d08ee374d055.mc{slot}"
# Default mcs/ directory. Override at runtime via `LEGAIA_MEDNAFEN_DIR`.
# (Left unset here so the manifest is portable.)

# ----------------------------------------------------------------------
# mc0 - v0.1 playthrough oracle anchor D: first frame of running
# battle (Tetsu tutorial fight).
#
# game_mode = 0x15 (BattleMode); active_scene stays "town01" because
# battles run as a scene-mode overlay on top of the loaded field scene,
# not a scene swap. The transitional `BattleInit (0x14)` lasts a single
# frame and is not captured here; mc0 is one frame later, in steady-
# state BattleMode where the battle action SM is ticking.
#
# Anchor in the v0.1 slice taxonomy (see backlog item 1): the
# comparison target for the recorded battle-start frame. The replay's
# `[[expected]]` block (once recorded) will name the frame at which
# the engine must reach scene_mode=Battle / active_scene=town01.
#
# Supersedes the FMV-trigger-corpus `fmv_trigger_str_8` capture that
# previously occupied this slot.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 0
label = "v0_1_battle_first_frame_tetsu"
ram_fingerprint_sha256 = "a6272a82f24bdee953e4f8dc19caa8df7621556e618d9bc9af7bc88712ad6f59"
description = "v0.1 slice anchor D: first frame inside the Tetsu tutorial battle (BattleMode running)"
topics = ["v0.1 slice", "battle init", "Tetsu tutorial", "BattleMode", "town01"]
phase = "battle"
# `expected_active_scene` is intentionally unset. `mode_trace_e3`
# treats that field as "engine reaches this scene_mode from a cold
# boot on this scene" -- which this anchor cannot satisfy (the engine
# boots into Title/town01, not Battle/town01). The actual scene under
# the battle IS "town01" with game_mode 0x15 BattleMode; the v0.1
# oracle picks this anchor up via [[expected]] frame comparisons in
# scripts/replays/v0_1_playthrough.toml when the field-VM-to-battle
# handoff exists in-engine.
expected_game_mode = 0x15

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------------
# mc1 - mid-battle with full 3-character active party (Vahn/Noa/Gala) vs
# 1 Gobu Gobu enemy, command menu open. game_mode = 0x15 (BattleMode).
# Useful as the canonical "battle with full party" oracle because all
# three battle-form character TMDs are simultaneously resident in
# `DAT_8007C018[0..=2]` (different shape from the field-form pack at
# PROT 0874 sect 0 - battle form is its own archive at PROT 1204 sect
# `other5`, asset type 0x09 "TMD2", 5 chunks: Vahn / Noa / Gala / 2 aux).
# ----------------------------------------------------------------------
[[scenarios]]
slot = 1
label = "party_battle_gobu_gobu"
backup_fingerprint = "a0c56d29eeeffd96aa809040bb807606545da792209b421274b9bea27c35e7f2"
description = "Mid-battle with Vahn/Noa/Gala active party vs one Gobu Gobu (game_mode 0x15 BattleMode, command menu open); all three battle-form character TMDs resident in DAT_8007C018[0..=2]"
topics = ["battle", "active party", "character meshes", "BattleMode", "DAT_8007C018", "battle-form TMD"]
phase = "battle"
expected_game_mode = 0x15

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios.watchpoints]]
label = "character_tmd_pointers"
start = 0x8007C018
end = 0x8007C02C
hint = "DAT_8007C018[0..=4]: 5 character TMD pointers (Vahn/Noa/Gala + 2 aux); battle-form addresses differ from field-form."

# ----------------------------------------------------------------------
# mc2 - per-STR FMV trigger corpus, slot 2 maps to STR 0 (fmv_id 0).
# fmv_id 0 plays \MOV\MV1.STR;1 (intro logo). This is also the same FMV
# that the title-screen attract loop fires after the idle countdown.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 2
label = "fmv_trigger_str_0"
ram_fingerprint_sha256 = "12fb95bba98f6da4946b599a5b2cb2683be62c8c215429e2a56b817301ed6b67"
description = "Per-STR FMV trigger captured for fmv_id=0 → MV1.STR (intro logo)"
topics = ["FMV trigger", "fmv_id 0", "MV1.STR"]
diff_against = [3]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios.watchpoints]]
label = "fmv_id_global"
start = 0x8007BA78
end = 0x8007BA7A
hint = "_DAT_8007BA78 = `00 00` (fmv_id = 0)."

[[scenarios.watchpoints]]
label = "next_game_mode_global"
start = 0x8007B83C
end = 0x8007B83D
hint = "_DAT_8007B83C = 0x1A (StrInit)."

[[scenarios.watchpoints]]
label = "field_pack_base_pointer"
start = 0x8007B8D0
end = 0x8007B8D4
hint = "_DAT_8007B8D0 = 0x8014BD30 (= 0x80139530 + 0x12800). map01's field-pack base; constant: `cutscene_trigger_corpus::MAP01_FIELD_PACK_BASE`."

# ----------------------------------------------------------------------
# mc3 - ROTATED to the chest-randomizer pre-scene-load capture
# `keikoku_chest_preload` (kingdom overworld map01, just outside the
# keikoku entrance). Its [[scenarios]] block lives with the keikoku chest
# captures further below. The slot previously held the FMV-trigger corpus
# capture `fmv_trigger_str_1` (fmv_id=1 -> MV3.STR first segment); no test
# references that label, so the block is superseded rather than kept.
# ----------------------------------------------------------------------

# ----------------------------------------------------------------------
# v0.1 playthrough oracle anchor C: clean pre-battle field state.
#
# Vahn standing in Rim Elm (`town01`) in front of his house, free
# movement, BEFORE talking to Tetsu and before the tutorial training
# battle. Game mode 0x03 - this empirically confirms that 0x03 is the
# field/town gameplay mode (corroborating the runtime pin on `map03`
# mode 0x03 in docs/subsystems/field-locomotion.md), NOT the "options
# menu" the dev mode-table label in engine-core::mode suggests.
#
# This is the clean cold-boot-reproducible town01 field anchor: the
# engine's `enter_field_live` drives a cold boot into Field/town01,
# which is exactly this state, so it is the convergence anchor for the
# v0.1 playthrough oracle's Phase-1 Field leg.
#
# LIBRARY-ONLY: its live slot has since been refreshed to hold anchor C+
# (the dialogue-accept frame, below). This anchor survives as its
# immutable content-hashed copy in saves/library/ and resolves via
# `backup_fingerprint`, so `slot` is the 255 "no live mednafen slot"
# sentinel. The v0.1 replay's `meta.scenario` points here and resolves
# the library copy in preference to any live slot, so the Field leg is
# unaffected.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 255
label = "v0_1_pre_battle_tetsu"
ram_fingerprint_sha256 = "cf9fede947e0269460b41f4773011edd7490dc4d202f041bced7cecc5bfcdde7"
backup_fingerprint = "4c650a3bb872b22029851a2a2e919fd8e3bb05eee19abaec9f5c82976424a779"
description = "v0.1 slice anchor C: Vahn in front of his house in Rim Elm, before talking to Tetsu / before the training battle (game_mode 0x03 field, town01)"
topics = ["v0.1 slice", "pre-battle", "field", "Tetsu tutorial", "town01"]
phase = "field"
expected_active_scene = "town01"
expected_game_mode = 0x03

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------------
# v0.1 playthrough oracle anchor C+: the pre-fight dialogue-accept frame.
#
# The chronological bridge between anchor C (free movement, before
# talking to Tetsu) and the battle anchors below: still at game mode 0x03
# (field) on `town01`, but now parked on the sparring partner's accept
# prompt. Advancing the dialogue one more step (the confirm press) runs
# the field-VM dialogue-accept that arms the scripted lone-Tetsu
# encounter and crosses into game mode 0x15 BattleMode.
#
# This is the mid-dialogue town01 frame the Battle-leg oracle needs to
# drive the Field -> Battle transition from real data rather than the
# engine's manual carrier-engage API. Because it is a field-mode state,
# `expected_active_scene = town01` is set and the auto-discover oracles
# can key off it (unlike the BattleMode anchors, which must skip).
# ----------------------------------------------------------------------
[[scenarios]]
slot = 6
label = "v0_1_tetsu_dialogue_accept"
ram_fingerprint_sha256 = "a7dddfc36cf9aa2733ef04735b42e68b86ddec2a0b3215e06507bd134ed4dc1b"
backup_fingerprint = "d3d14657aabafdeeb5ee8503091a25ef1aa1dd56974a8c89e75bbcd79bc22011"
description = "v0.1 slice anchor C+: town01 field (game_mode 0x03), sparring-partner dialogue at the accept prompt; the confirm press arms the scripted lone-Tetsu fight and enters BattleMode"
topics = ["v0.1 slice", "field dialogue", "dialogue accept", "Tetsu tutorial", "scripted encounter", "town01"]
phase = "field"
expected_game_mode = 0x03
expected_active_scene = "town01"

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------------
# The Tetsu tutorial battle progression (anchors D0 / D / D+ / D++).
#
# Four save states captured in chronological order through the scripted
# Tetsu tutorial fight, all at game mode 0x15 BattleMode on top of
# `town01`:
#
#   D0  battle scene loading - screen black, battle flag already
#       flipped (game_mode 0x15), graphics not yet drawn.
#   D   battle graphics visible; Tetsu's pre-fight dialogue.
#   D+  top-level command menu (Fight / Run).
#   D++ command submenu (Spirit / Item / Attack).
#
# Followed by anchor E (post-victory town dialogue, back at game_mode
# 0x03 field). Together they are the retail ground-truth sequence for
# the v0.1 oracle's deferred Battle leg.
#
# LIBRARY-ONLY: these were captured in live slots mc4/mc5/mc6 but those
# slots have since been refreshed to other content. They survive only as
# the immutable, content-hashed copies in saves/library/ and resolve via
# `backup_fingerprint`, so `slot` is set to 255 (the "no live mednafen
# slot" sentinel) - re-capturing them into a live slot is not required.
#
# `expected_active_scene` is intentionally unset on all four: the battle
# runs as a scene-mode overlay on the loaded field scene, and the engine
# cannot cold-boot directly into BattleMode, so the auto-discover oracles
# (mode_trace / vram / pcm / audio, which key off `expected_active_scene`)
# must skip them. The overlay_slice covers the RAM-overlay region where
# the battle action SM lives.
# ----------------------------------------------------------------------

# D0 is mc7 (still a live slot); see its block further below.

# anchor D: battle graphics visible, Tetsu pre-fight dialogue.
[[scenarios]]
slot = 255
label = "v0_1_battle_start_tetsu"
ram_fingerprint_sha256 = "66bef6b52a5e1d98f7cf3b06fa4c030fa2967a143736b95e371d5fb4e8a3d23c"
backup_fingerprint = "a4673cb5bfa57ab5135820b846cf2d227f69ce4c3dadb613cebc6df588d80cd7"
description = "v0.1 slice anchor D: Tetsu tutorial battle, graphics visible (past loading), Tetsu pre-fight dialogue; BattleMode over town01"
topics = ["v0.1 slice", "battle init", "Tetsu tutorial", "BattleMode", "town01"]
phase = "battle"
expected_game_mode = 0x15

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# anchor D+: top-level battle command menu (Fight / Run).
[[scenarios]]
slot = 255
label = "v0_1_battle_command_menu"
ram_fingerprint_sha256 = "253a180e33ce95050095d699e4512a0b9fdc798c218947807e2d9268a6733d81"
backup_fingerprint = "42845402539bfda659b9e17dd2d82749749cccffdb36882243bcde19d230f04e"
description = "v0.1 slice anchor D+: Tetsu battle, top-level command menu open (Fight / Run)"
topics = ["v0.1 slice", "battle command menu", "Tetsu tutorial", "BattleMode", "town01"]
phase = "battle"
expected_game_mode = 0x15

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# anchor D++: battle command submenu (Spirit / Item / Attack).
[[scenarios]]
slot = 255
label = "v0_1_battle_command_submenu"
ram_fingerprint_sha256 = "e6d05047346bac2b8e241bb8b711fdb071419687c017b4f6d06e7b23148e4f12"
backup_fingerprint = "281c7a3ac1497bdf5365f58d35d93b10b14d1268a2bad2e103cc1e232517e289"
description = "v0.1 slice anchor D++: Tetsu battle, command submenu open (Spirit / Item / Attack options)"
topics = ["v0.1 slice", "battle submenu", "Tetsu tutorial", "BattleMode", "town01"]
phase = "battle"
expected_game_mode = 0x15

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------------
# mc7 - v0.1 playthrough oracle anchor D0: battle scene loading.
#
# The earliest of the Tetsu-battle captures: the screen is black and
# the battle scene is mid-load. The battle flag is already flipped -
# `_DAT_8007B83C` (game mode) reads 0x15 BattleMode - but the battle
# graphics are not yet drawn. Chronologically this precedes mc4 (D),
# which is the first frame with battle graphics visible.
#
# `expected_active_scene` is intentionally unset (battle anchor; see the
# Tetsu-progression header above): the engine cannot cold-boot into
# BattleMode, so the auto-discover oracles skip it.
#
# Supersedes the FMV-trigger-corpus `fmv_trigger_str_5` capture that
# previously occupied this slot.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 7
label = "v0_1_battle_loading_tetsu"
ram_fingerprint_sha256 = "5962a9313814c6ec1963fd2b495dabdd2a7ba2fa6bfc6a72310275f548bdf0a5"
backup_fingerprint = "5649c05e0b96b87758cdfc204995efc60dda009baaa4a783b6df8c0cb1010b0a"
description = "v0.1 slice anchor D0: Tetsu battle scene loading - screen black, battle flag set (game_mode 0x15), graphics not yet drawn"
topics = ["v0.1 slice", "battle init", "battle loading", "Tetsu tutorial", "BattleMode", "town01"]
phase = "battle"
expected_game_mode = 0x15

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------------
# mc8 - v0.1 playthrough oracle anchor E: post-victory town dialogue.
#
# *After* the Tetsu tutorial battle ends: the player is back in Rim Elm
# (`town01`) at game mode 0x03 (field / MainMode), talking to Tetsu in
# the town. This is the post-battle field state - the tail end of the
# v0.1 slice, after the BattleMode anchors (mc7 / mc4 / mc5 / mc6).
#
# Unlike the battle anchors this IS a field scene the engine can
# cold-boot into, so it keeps `expected_active_scene = town01` and is
# picked up by the auto-discover field oracles.
#
# Supersedes the FMV-trigger-corpus `fmv_trigger_str_6` capture.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 8
label = "v0_1_post_battle_tetsu_town"
ram_fingerprint_sha256 = "0b42d20e47f16c1d7e6fbf97b07b61bada3965640819a1ae03d29a5bb2f34fb7"
backup_fingerprint = "1b1d645f8f8827c97f678d214fe16d273153313872ef8ce29b426baeffbf8f84"
description = "v0.1 slice anchor E: post-victory, back in Rim Elm talking to Tetsu (game_mode 0x03 field, town01)"
topics = ["v0.1 slice", "post-battle", "Tetsu tutorial", "MainMode", "town01"]
phase = "field"
expected_active_scene = "town01"
expected_game_mode = 0x03

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------------
# mc9 - fmv_id=7. Maps to slot[7] which points at \DATA\MOV.STR;1 (cut).
# ----------------------------------------------------------------------
[[scenarios]]
slot = 9
label = "fmv_trigger_str_7"
description = "Per-STR FMV trigger captured for fmv_id=7 (cut path slot)"
topics = ["FMV trigger", "fmv_id 7", "cut path slot"]
diff_against = [0]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios.watchpoints]]
label = "fmv_id_global"
start = 0x8007BA78
end = 0x8007BA7A
hint = "_DAT_8007BA78 = `07 00` (fmv_id = 7). Slot[7] points at \\DATA\\MOV.STR;1 - cut path."

# ----------------------------------------------------------------------
# Boot-to-first-town arc checkpoints (mednafen-side slot not allocated;
# these are primarily PCSX-Redux save-state references for the
# step-by-step launch -> first town reverse-engineering walk).
#
# Each scenario names a stable game state along the boot path. Capture
# the state in PCSX-Redux (Ctrl+F<N>), then add the resulting
# `pcsx_redux_sstate` field below. `manage-states.py list` shows "·"
# when the manifest declares a path but the file is missing, "•" when
# the file exists - so an unfilled checkpoint surfaces as a TODO until
# captured.
#
# The slot field is set to 255 (= sentinel "no mednafen slot allocated")
# so the existing slot-based tests don't accidentally pick these up.
# Allocate a real mednafen mc{0..9} slot if/when a mednafen capture of
# the same state is added.
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "cold_boot_pre_init"
description = "Save state taken pre-BIOS-init; cold-boot the title overlay LZS decode + countdown trigger from here."
phase = "boot"
pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate7"
topics = ["boot path", "title overlay load", "countdown trigger"]

[[scenarios]]
slot = 255
label = "publisher_logos"
description = "Mid publisher-logo sequence (Prokion / Contrail / SCEA / WARNING). init.pak TIMs visible in VRAM; PROT 0895 has been LZS-decoded into the staging buffer."
phase = "boot"
# pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate3"   # uncomment after capture
topics = ["boot path", "init.pak", "publisher_logos"]

[[scenarios]]
slot = 255
label = "title_attract"
description = "Title screen idle - countdown ticking; main TITLE TIM uploaded to VRAM; tick function FUN_801DD35C running."
phase = "title"
# pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate4"   # uncomment after capture
topics = ["title overlay", "countdown trigger", "attract"]

[[scenarios]]
slot = 255
label = "title_menu_idle"
description = "Title menu visible (NEW GAME / CONTINUE). The user has interrupted the attract countdown but not yet pressed X; menu-glyph atlas at 0x80106478 is live; sub-mode VM in title-overlay tick is on the menu branch."
phase = "title"
# (live slot reused for the rim_elm_queen_bee_battle capture; re-capture to a
# fresh slot + back it up to re-attach a durable reference here)
topics = ["title overlay", "title menu", "menu glyph atlas"]

[[scenarios]]
slot = 255
label = "save_select_idle"
description = "Save-slot select screen open (CONTINUE pressed). Menu overlay 0896 paged in; FUN_801DC6B4 driving the save-screen SM."
phase = "menu"
# pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate5"   # uncomment after capture
topics = ["save screen", "menu overlay"]

# ----------------------------------------------------------------------
# NEW GAME flow: title menu -> intro cutscene A -> Rim Elm zoom-in ->
# Vahn walks out of house -> name input UI -> intro cutscene B ->
# free-roam first town. Each is a stable hold point for capture.
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "new_game_cutscene_intro_a"
description = "First in-game (engine-rendered, NOT STR-video) cutscene after the player picks NEW GAME: low-poly actors around the Genesis tree with subtitle text (\"It was the Seru.\"). Engine-side opening narrative leading into Rim Elm."
phase = "cutscene"
backup_fingerprint = "1966a3c192fa0c0c3a8bfb7dff38256c2b06563ff8c5df0642a35bf6d6975c65"
topics = ["new game", "in-engine cutscene", "intro"]

[[scenarios]]
slot = 255
label = "rim_elm_zoom_intro"
description = "Establishing 3D camera view of Rim Elm (town on a cliff above the sea, Vahn's house among the huts); in-engine cinematic, not a regular field scene. Distinct camera path / actor scripting from interactive Rim Elm."
phase = "field"
backup_fingerprint = "35927453f61bb246369fb0c991cbd76326c5afb08e56da91bbc1134d1cfca357"
topics = ["new game", "Rim Elm intro", "cinematic camera"]

[[scenarios]]
slot = 255
label = "vahn_walks_out"
description = "Vahn (player model, blue hair) standing on the step outside his house at the start of the intro sequence. Actor VM driving a fixed motion path."
phase = "field"
backup_fingerprint = "9e9448e8a5aab75e393638340a4b22a6b327418439be3ee658672917a0c2b413"
topics = ["new game", "scripted actor motion", "intro"]

[[scenarios]]
slot = 255
label = "name_input_ui"
description = "Name-entry UI open (\"Select your name.\", default 'Vahn'). Character grid: A-Z / a-z / 0-9 / symbols, with BS + Select; portrait of Vahn at left. Distinct menu overlay; text-entry SM with character grid + cursor. Fires AFTER the field opens (intro -> Rim Elm -> Vahn outside house -> THIS), not in the title front-end."
phase = "menu"
backup_fingerprint = "a14afa511de1abef6399abebf792e9f5b7ea669d4d60ad48e15be0b53b341a68"
topics = ["new game", "name input", "menu overlay"]

[[scenarios]]
slot = 255
label = "new_game_cutscene_intro_b"
description = "Second in-game cutscene immediately after name confirmation; final lead-in to free-roam Rim Elm."
phase = "cutscene"
# pcsx_redux_sstate = "..."   # uncomment after capture
topics = ["new game", "in-engine cutscene", "intro"]

[[scenarios]]
slot = 255
label = "first_town_interactive"
description = "Rim Elm free-roam after the full NEW GAME intro completes (or CONTINUE drops the player here from a near-start save). Dialog VM idle, player input live. End state of the boot-to-town walk."
phase = "field"
expected_active_scene = "town01"
# pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate9"   # uncomment after capture
topics = ["field", "town01", "Rim Elm", "interactive"]

# ----------------------------------------------------------------------
# CONTINUE flow: an alternate path from save_select_idle that skips
# the NEW GAME intros and drops the player at whatever save state they
# loaded. field_load_first_town captures the loader frame; from there
# the player is already inside first_town_interactive (or wherever).
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "field_load_first_town"
description = "Mid-transition to the first field scene via either NEW GAME (after name input) or CONTINUE. FUN_8001F7C0 scene-asset loader executing; field-pack bytes streaming into _DAT_8007B8D0."
phase = "field"
# pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate6"   # uncomment after capture
topics = ["field load", "scene asset loader", "town01"]

# ----------------------------------------------------------------------
# Walkable field scene with a fully-painted collision grid - the state
# the field free-movement controller (FUN_801d01b0) was runtime-pinned
# on. Captured live as PCSX-Redux SCUS94254.sstate6 and BACKED UP to the
# immutable library (the live slot is overwritten by any later save).
#
# Verified via scripts/pcsx-redux/autorun_town01_script_flow.lua:
#   - field buffer _DAT_1f8003ec = 0x80139530
#   - collision grid (+0x4000) has 7455 / 16384 wall tiles painted
#   - steady state runs ONE field-VM context: script_id 0xFB (system
#     channel), bytecode at 0x8010F092, looping pc 0x0102..0x0297 - the
#     same per-frame loop the clean-room engine's static trace settles
#     into, and the nibble-7 paint ops fire 0 times (walls are painted
#     at scene-load, not per frame).
# Use this as the reference state for collision-grid / locomotion parity.
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "field_walled_collision_pin"
backup_fingerprint = "35e640510858f62cf07db77975d0e434466fa9fa079a40a23ea8adb8c30e78ea"
description = "Walkable field scene, game_mode 0x03, collision grid +0x4000 has 7455 painted wall tiles; system script 0xFB loops pc 0x102..0x297. Runtime-pin save for FUN_801d01b0 locomotion + collision parity."
phase = "field"
expected_game_mode = 0x03
topics = ["field", "collision grid", "locomotion", "FUN_801d01b0", "nibble-7 paint"]

# ----------------------------------------------------------------------
# Pre-transition save: player stands in Drake Castle (a field scene); a
# brief Up press walks into a scene exit that transitions to the Drake
# Kingdom world map. Captured live as PCSX-Redux SCUS94254.sstate1 and
# backed up immutably. Use this to capture the scene-LOAD script flow:
# replay it driving Up so FUN_8003aeb0 / FUN_8003ab2c / the nibble-7
# collision-paint sites fire during the transition (the load-time flow a
# steady-state save can't show).
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "drake_castle_to_worldmap"
backup_fingerprint = "0f659b1f3186f549a2cbac7617b4e5597c5bf4ba91e4f9c8fefe38c4e65b58a5"
description = "Drake Castle field scene, ~half-second Up press triggers a transition to the Drake Kingdom world map. Pre-transition save for capturing scene-load script flow (FUN_8003aeb0 + nibble-7 wall paints)."
phase = "field"
topics = ["field", "scene transition", "world map", "Drake", "scene-load", "nibble-7 paint"]

# ----------------------------------------------------------------------
# Sebucus + Karisto kingdom-warp pre-transition saves (PCSX-Redux),
# the second + third kingdoms to complement drake_castle_to_worldmap.
# Each is a town field scene one held-Down press away from warping to
# that kingdom's overworld. Used to confirm the slot-4 in-place render
# reads (world-map renderer 0x801F78D4 + cluster-A GTE prim path)
# generalise across kingdoms; see docs/formats/world-map-overlay.md.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 255
label = "octam_to_sebucus_worldmap"
backup_fingerprint = "f126470502fe484356fa456d691269d9fd35c3a38491b88145e9b0ac3e535e78"
description = "Octam field scene (Sebucus Islands); a ~1s Down press warps to the Sebucus world map (map02, PROT 0244). Pre-transition PCSX-Redux save for capturing the kingdom-warp scene-load + the slot-4 in-place render reads across a second kingdom."
phase = "field"
topics = ["field", "scene transition", "world map", "Sebucus", "map02", "slot-4", "scene-load"]

[[scenarios]]
slot = 255
label = "sol_to_karisto_worldmap"
backup_fingerprint = "3643191374fe98ae975d8f2b83806bf446a2693881dfeac362e76277541012ff"
description = "Sol field scene (Karisto Kingdom); a ~1s Down press warps to the Karisto world map (map03, PROT 0391). Pre-transition PCSX-Redux save for capturing the kingdom-warp scene-load + the slot-4 in-place render reads across a third kingdom."
phase = "field"
topics = ["field", "scene transition", "world map", "Karisto", "map03", "slot-4", "scene-load"]

# ----------------------------------------------------------------------
# Rim Elm (town01) pre-transition save: a ~1s Up press walks into the
# south exit and warps to the Drake Kingdom overworld (map01). Ground-
# truth for the door (scene-transition) randomizer: town01's single exit
# is the field-VM 0x3F named-scene-change at MAN op 0x6f95 (-> map01,
# index 85). The randomizer's runtime oracle
# (door_randomizer_runtime_e2e) patches this exit to a different scene
# and confirms the engine warps to the patched destination.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 9
label = "door_warp_town01_to_map01"
backup_fingerprint = "7ada4ac83d183d7cd5ed55f6c8aae96e6cae00b9016a3194a92b066f34402206"
description = "Rim Elm (town01) field; a ~1s Up press walks into the south exit and warps to the Drake Kingdom overworld (map01). Pre-transition save pinning town01's single exit = the 0x3F named-scene-change at MAN op 0x6f95 (-> map01, index 85). Ground-truth for the door randomizer + its runtime oracle."
phase = "field"
topics = ["field", "scene transition", "door randomizer", "0x3F", "town01", "map01"]
# The capture session's Rim Elm is the later-story `town0c` scene variant
# (the save's pool slot 0 reads "town0c", same as the Mei-house captures
# from the same session) -- not the pre-Tetsu `town01` variant the label
# implies. The mode-trace oracle boots the scene actually in the capture.
expected_active_scene = "town0c"

# ----------------------------------------------------------------------
# Rim Elm Mei's-house door RE set. Entering a house here is an INTRA-SCENE
# REPOSITION, not a scene change: the active scene-name buffers
# (0x8007050C / 0x80084548) stay "town0c" across the transition, while the
# player struct (0x80083794) position jumps to the interior sub-area. So
# this is a different mechanism from the 0x3F named-scene-change the door
# randomizer handles (town0c's MAN has no 0x3E warp and one 0x3F, to map01).
#   door_warp_rim_elm_to_mei_house  mednafen slot 0  outside (pre), town0c
#   mei_house_inside                mednafen slot 1  inside (post), town0c
#   mei_house_door_pcsx             pcsx sstate1     outside, earlier town01 variant (trace input)
#   mei_house_inside_pcsx           pcsx sstate2     inside,  earlier town01 variant
# (The PCSX pair is an earlier game stage / Rim Elm variant than the
# mednafen pair, so positions differ; the door MECHANISM is the same.)
# ----------------------------------------------------------------------
[[scenarios]]
slot = 0
label = "door_warp_rim_elm_to_mei_house"
backup_fingerprint = "a6e3ca59ca5849561325cb94cc660b991b75259e7495fe740e873f4a9b0a6387"
description = "Rim Elm (town0c) field, standing directly beneath an open house door; a ~1s Up press enters Mei's house. Pre-transition save for RE'ing the intra-town (house / interior) door-warp mechanism -- an intra-scene reposition (scene stays town0c), distinct from the 0x3F named-scene-change the door randomizer handles."
phase = "field"
topics = ["field", "intra-town door", "house warp", "door randomizer", "town0c"]
expected_active_scene = "town0c"

[[scenarios]]
slot = 1
label = "mei_house_inside"
backup_fingerprint = "8d5239ec0434644fa25e0660941dc0096b8e3857d4f9826abfdfd8ac2706d64d"
description = "Rim Elm Mei's-house INTERIOR (mednafen), one step after entering from door_warp_rim_elm_to_mei_house. Scene is still town0c; only the player position changed -- confirms the house door is an intra-scene reposition, not a scene change."
phase = "field"
topics = ["field", "intra-town door", "house interior", "town0c"]
expected_active_scene = "town0c"

[[scenarios]]
slot = 255
label = "mei_house_door_pcsx"
backup_fingerprint = "3c118f4a26bf0be04ab18f08b15703caa97e77c47be3ac94b8730215dc91d3f7"
description = "Rim Elm standing beneath a house door, PCSX-Redux (earlier game stage / town01 variant than the mednafen pair). Trace input for the intra-town door mechanism: hold Up to enter and watch the field VM."
phase = "field"

[[scenarios]]
slot = 255
label = "mei_house_inside_pcsx"
backup_fingerprint = "df2723f6f9ebd46f6b13e9711f636e4c47bd4fd7a96fa71de9272d53a30f56ec"
description = "Rim Elm house interior, PCSX-Redux (earlier game stage / town01 variant), one step after entering. Pair with mei_house_door_pcsx."
phase = "field"

# ----------------------------------------------------------------------
# Pre-transition save: player stands on a walkable area; a brief Down
# press walks into a town entrance that loads a standalone town scene.
# Use this to capture a town scene's MAN load: replay it driving Down so
# the asset dispatcher FUN_8001F05C fires its MAN (type 0x03) dispatch
# during the transition. This capture pinned the runtime MAN source
# `_DAT_8007b898` -> the count=6 scene_asset_table variant in the town's
# own PROT block (see autorun_man_source.lua + docs/formats/scene-bundles.md).
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "rim_elm_queen_bee_battle"
backup_fingerprint = "3d22fa5fd53d47cd22999a7b377ec8ece057fdb5ca164357be0f96a65147ddf3"
description = "Queen Bee boss-ambush battle in Rim Elm (town01). The fight auto-starts a few seconds after load and is lost in ~15s (no game over). Battle-init runs the monster archive loader (FUN_800542C8 -> FUN_80054CB0), so this is the capture point for the monster stat-record source + format validation. Monster ids overlap town01's encounter formations {4,7,10,61,62,63,79}."
phase = "battle"
topics = ["battle", "monster record", "monster stats", "FUN_80054CB0", "FUN_800542C8", "monster_data", "encounter"]

[[scenarios]]
slot = 255
label = "rim_elm_gimard_victory"
backup_fingerprint = "eaa8d800571dd9c144bcac7c740d1f9dcbeae4b0f7787a4b3e07acd1e82f1a73"
description = "Rim Elm (town01) scripted Gimard fight captured mid-combo, just before Gimard's death - the fight resolves on its own (no input) into the victory-spoils path. Capture point for the battle reward path (EXP / gold / drop): write-watchpoints on the XP accumulator 0x80084440, party gold 0x8008459C, and party XP bank 0x800845A4 catch the per-monster reward summation. Gimard is a lone enemy, so the staged totals are his EXP/gold. Drives autorun_battle_reward_source.lua."
phase = "battle"
topics = ["battle", "reward", "exp", "gold", "drop", "spoils", "FUN_80026018", "monster record"]

# ----------------------------------------------------------------------
# Seru-capture (spell learn) before/after pair, captured during the Rim
# Elm Gimard fight. `before` is the frame just prior to the capture; `after`
# is during the "captured!" banner. Diffing the two pins the persistent
# spell-learn write into the character record: ONLY 3 bytes change in the
# whole 4-record character table, all in Vahn's record (base 0x80084708):
#   +0x13C  00 -> 01    spell-list count 0 -> 1
#   +0x13D  00 -> 0x81  spell-id array[0] = 0x81 (Gimard's spell)
#   +0x161  00 -> 01    spell-level array[0] = 1
# This validates legaia_save::CharacterRecord::spell_list (count @ +0x13C,
# ids @ +0x13D, levels @ +0x161) against retail, and shows a story-Seru
# capture is an IMMEDIATE grant (no capture-points accumulation in the
# record) at spell level 1. Real spell ids (0x81) sit far above the
# clean-room SpellCatalog approximations. See
# capture_observations::seru_capture.
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "rim_elm_gimard_seru_capture_before"
backup_fingerprint = "3edf5fb14cd0687603febd774cc1a5e197a9f38e711d089bf5c9c02fa66dac5b"
description = "Rim Elm (town01) Gimard fight, frame just before the Seru capture lands. Vahn's spell list at record +0x13C is still all-zero. Pair with rim_elm_gimard_seru_capture_after to diff the spell-learn write."
phase = "battle"
topics = ["battle", "seru", "capture", "spell learn", "spell_list", "Gimard", "character record"]

[[scenarios]]
slot = 255
label = "rim_elm_gimard_seru_capture_after"
backup_fingerprint = "7f91f1f0d2b3504b528a66b43fdb052a6e4f54421240b7c457a7eb34fe6a2810"
description = "Rim Elm (town01) Gimard fight, during the 'captured!' banner. Vahn's spell list (record +0x13C) now reads count=1, id[0]=0x81, level[0]=1. Capture point for the persistent Seru spell-learn write. See capture_observations::seru_capture."
phase = "battle"
topics = ["battle", "seru", "capture", "spell learn", "spell_list", "Gimard", "character record"]

[[scenarios]]
slot = 255
label = "overworld_into_town_man_load"
backup_fingerprint = "bdbacd378093e730c0967a0c27822955f7561875c6c8e0adb2881fc1bc42f597"
description = "Walkable area; a ~0.75s Down press walks into a town entrance, loading a standalone town scene. Pre-transition save for capturing the town MAN load via the asset dispatcher (FUN_8001F05C case 3 -> _DAT_8007b898)."
phase = "field"
expected_game_mode = 0x03
topics = ["field", "scene transition", "town", "MAN", "scene_asset_table", "FUN_8001F05C", "_DAT_8007b898"]

# ----------------------------------------------------------------------
# Title screen, NEW GAME row highlighted. One Cross press short of the
# title -> field-launch transition: confirming writes master game-mode
# 0x02 (FUN_80025B64), whose field scene init FUN_801D6704 hands off to
# 0x03. Pre-launch anchor for the New Game boot chain; see
# docs/subsystems/boot.md "New Game boot chain".
# ----------------------------------------------------------------------
[[scenarios]]
slot = 5
label = "title_screen_new_game"
ram_fingerprint_sha256 = "9efbfeacdbb7f98ab7d0f9eee62468e0ec05f540677498f771eda113c9a6776c"
backup_fingerprint = "71094451e85eb2ecef11d2fcc975615360fb3bf58b506467fb8e1aa01f4c0334"
description = "Title screen with the NEW GAME row highlighted; one Cross press before the title -> field master-mode transition (FUN_80025B64 -> FUN_801D6704 -> game_mode 0x03). Pre-launch anchor for the New Game boot chain."
phase = "boot"
topics = ["title screen", "new game", "boot", "FUN_801DD35C", "master game-mode", "FUN_80025B64", "FUN_801D6704"]

# ----------------------------------------------------------------------
# Battle 2D-billboard hit-spark + summon-flicker captures. The melee
# hit-spark frame is the capture that resolved the long-open "what fills
# the efect.dat atlas page (0,0)" question: walking the live GPU prim
# pool shows NO prim samples page (0,0) / 8bpp / tpage 0x7680 anywhere -
# the static atlas tpage is an authoring placeholder, and the runtime
# relocates each billboard onto the loaded effect-texture pages. The
# white melee impact draws as textured quads (POLY_FT4/GT4) sampling the
# PROT 870 flame-atlas pages (320,0) / (448,0) with effect-band CLUTs
# (rows 473..480) - pages that appear ONLY in the impact frame (absent
# from a no-spark command-menu frame), confirming they are the effect.
# See docs/formats/effect.md "2D sprite billboards".
#
# The two Gimard Tail-Fire frames are consecutive-ish summon-cast frames
# for the remaining flame-flicker thread; their effect-CLUT-band VRAM
# data (rows 473..481) is byte-identical, which points the flicker at
# per-frame prim CBA-column cycling rather than CLUT-data re-upload.
# ----------------------------------------------------------------------

[[scenarios]]
slot = 255
label = "battle_melee_hit_spark"
backup_fingerprint = "2962ebbb70cc410de1050c0e4f231ce9ad66b5a509dc46d5649cff5c8b721581"
phase = "battle"
description = "Rim Elm (town01) battle frame on the impact of a melee attack, with the small white hit-spark effect on screen. Capture point for the efect.dat 2D-billboard texel source: the live GPU prim pool shows the spark drawn as textured quads sampling the PROT 870 flame-atlas pages (320,0)/(448,0) (effect-band CLUTs), with NO prim sampling page (0,0) / 8bpp / tpage 0x7680 - falsifying the page-(0,0) atlas reading."
topics = ["battle", "effect", "2D billboard", "efect.dat", "hit spark", "PROT 870", "FUN_801E0088", "prim pool"]

[[scenarios]]
slot = 255
label = "battle_gimard_tail_fire_a"
backup_fingerprint = "c8038c2d86f84e42c0c2148fbf499eb795b645689e7260e88f7065b5c6a7c935"
phase = "battle"
description = "Rim Elm (town01) Gimard Tail Fire summon mid-cast, flame-animation frame A. Pair with battle_gimard_tail_fire_b for the flame-flicker thread (the PROT 905 summon overlay's per-frame animation). The effect-CLUT band (rows 473..481) is byte-identical between A and B, indicating the flicker is prim CBA-column cycling, not CLUT-data re-upload."
topics = ["battle", "effect", "summon", "Tail Fire", "Gimard", "CLUT cycling", "PROT 905"]

[[scenarios]]
slot = 255
label = "battle_gimard_tail_fire_b"
backup_fingerprint = "def8234c3edec96f769a3a6e3cde0e5b93f7b68296f236ea01aba56a0f0637b1"
phase = "battle"
description = "Rim Elm (town01) Gimard Tail Fire summon mid-cast, flame-animation frame B. Pair with battle_gimard_tail_fire_a (see its note) for the flame-flicker thread."
topics = ["battle", "effect", "summon", "Tail Fire", "Gimard", "CLUT cycling", "PROT 905"]

# ----------------------------------------------------------------------
# Single-level-up capture set: a 3-party (Vahn/Noa/Gala) battle in which
# Noa levels up exactly once, from L2 to L3. Four ordered frames isolate
# one application of the level-up stat-growth applier FUN_801E9504 (one
# curve byte -> one stat delta), which the multi-level corpus triplets
# cannot do - they're the reconciliation set for the open gain-arithmetic
# discrepancy in docs/subsystems/level-up.md (the literal
# (max-start)*curve[row][level-1]/0x24C0 term overshoots the captured
# 4-level deltas ~4.3..4.8x). Noa is growth slot 1; L2->L3 reads
# curve[row][1]. All four are immutable library backups (slot 255).
# ----------------------------------------------------------------------
[[scenarios]]
slot = 255
label = "noa_levelup_field_pre"
ram_fingerprint_sha256 = "d69feeebb9fc8c74fcdc224c5aa249a5b593292d3b03e06a3e29056133b4299c"
backup_fingerprint = "9c09d402c52351514ab8051d82ba4ba2992a6e5f782f61c1114be02eca247ed3"
phase = "field"
description = "Field, just before the 3-party (Vahn/Noa/Gala) battle that levels Noa from L2 to L3. The pre-battle persistent character record (Noa's L2 stats at record +0x11C..+0x12D) - the baseline for the single-level stat-growth diff."
topics = ["level-up", "stat growth", "Noa", "FUN_801E9504", "field", "pre-level"]

[[scenarios]]
slot = 255
label = "noa_levelup_fight_pre"
ram_fingerprint_sha256 = "1f9999982383f15985d97f171d3c2c3e8561a30e7fb090c953e2baff6c4a253c"
backup_fingerprint = "ceced9566e2068009cc5b75bf0ff3689045fd75f962b9076f3295ba23967447e"
phase = "battle"
description = "Mid-battle (Vahn/Noa/Gala active party), before the level-up resolves. Noa still at L2. Pairs with noa_levelup_banner / noa_levelup_field_post for the single-level (L2->L3) stat-growth reconciliation."
topics = ["level-up", "stat growth", "Noa", "FUN_801E9504", "battle", "pre-level"]

[[scenarios]]
slot = 255
label = "noa_levelup_banner"
ram_fingerprint_sha256 = "6652dcb2b96f1b21d88eb7e8cef8d21703d32c8c7cc8e9279585e6174984e434"
backup_fingerprint = "f783d727ccfbe8f7341740a2a6ee5a46866adedda03d273403b06ceb2a87c625"
phase = "battle"
description = "End-of-battle level-up banner visible: Noa leveling from L2 to L3. The frame where FUN_801E9504 has applied the growth and the banner displays the gains."
topics = ["level-up", "stat growth", "Noa", "FUN_801E9504", "battle", "level-up banner"]

[[scenarios]]
slot = 255
label = "noa_levelup_field_post"
ram_fingerprint_sha256 = "05aad8d4ebde0d35f392e4d5ea498482bfd272b28c803ce292c222f903c77132"
backup_fingerprint = "de476a2791fba517ffddd4c23c92ce89cd03d52c69b47e602f85a3b7237e5296"
phase = "field"
description = "Field, after the battle and the L2->L3 level-up have settled. Noa's L3 persistent character record (+0x11C..+0x12D). Diff against noa_levelup_field_pre for the byte-exact single-level stat deltas that reconcile the FUN_801E9504 gain arithmetic."
topics = ["level-up", "stat growth", "Noa", "FUN_801E9504", "field", "post-level"]

# ----------------------------------------------------------------
# Gimard player-summon ("Burning Attack") capture states. Vahn is the
# solo caster. Three phases across the cast; the live summon-part mesh
# rotation window is the Burning Attack frame. These back the
# FUN_801F7088 rotation-source probe (autorun_summon_rotation.lua).
# NOTE: the PLAYER Gimard move is "Burning Attack"; the ENEMY Gimard
# boss move is "Fire Tail" - distinct moves with distinct animations.
# ----------------------------------------------------------------

[[scenarios]]
label = "gimard_summon_start"
slot = 255
pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate5"
backup_fingerprint = "e7a26939600744e64577be4f3f5868f91a46fadbcd8d70baf90395202ac602c5"
description = "Player Gimard Seru-magic cast, first frames of the summon (Vahn solo caster, no party). Pre-mesh-visible; the summon scene-graph spawn-stager window."

[[scenarios]]
label = "gimard_summon_visible"
slot = 255
pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate6"
backup_fingerprint = "8aa0ee1a261277bc987b57eaf283fcd074474601abbefd3d4593e5725009a0d4"
description = "Player Gimard summon with the mesh visible on screen, pre-attack (Vahn solo caster). Steady-state summon render; resident summon overlay + per-part transform target."

[[scenarios]]
label = "gimard_burning_attack"
slot = 255
pcsx_redux_sstate = "$HOME/Tools/pcsx-redux/SCUS94254.sstate7"
backup_fingerprint = "8653d14b3efae4e102e98b1e03d594d9a013baa028f4f481955d7e9a486d3050"
description = "Player Gimard performing the Burning Attack move (move text + fire animation on screen, Vahn solo caster). The live summon-part mesh-rotation window for the FUN_801F7088 source hunt."

# ----------------------------------------------------------------
# Treasure-chest give-item capture pair (scene "keikoku" = Ravine).
# Slot 1 = standing in front of an unopened chest; slot 2 = the open-
# chest action just starting (chest-open SFX queued, item not yet
# granted). The scene MAN loads verbatim at RAM 0x8012f4c8; the four
# chest actors' interaction records carry field-VM GIVE_ITEM (0x39)
# ops after their announcement dialogue. The actor that advances its
# open state between the two saves is the Phoenix chest (item 0x80).
# Ground truth for the chest-randomizer dialogue-skipping walk.
# ----------------------------------------------------------------

[[scenarios]]
slot = 1
label = "keikoku_chest_pre"
ram_fingerprint_sha256 = "f88dd9fc09e13f70772030bb5bfd296b6d13815ddb8fa31c9f7880b0d1a742d1"
backup_fingerprint = "5c450953a321496e00c7410ba49f46e2d596beb983a863f74023cb8cf8e5f26a"
phase = "field"
expected_active_scene = "keikoku"
description = "Field, standing directly in front of an unopened treasure chest in keikoku (Ravine). Pre-open snapshot for the chest give-item RE."
topics = ["chest", "give-item", "field-VM 0x39", "randomizer", "keikoku"]

[[scenarios]]
slot = 2
label = "keikoku_chest_open"
ram_fingerprint_sha256 = "ae775412134d561076123a8be7548a6bedd810efd2caa53c9315682a09dd53ff"
backup_fingerprint = "c56db98557c3bae4066e8b42acda38827937d90442be53e610a4bdc74e1bf440"
phase = "field"
expected_active_scene = "keikoku"
description = "Field, the open-chest action just starting (chest-open SFX 0x2d queued, item not yet added). Diff against keikoku_chest_pre: the Phoenix (0x80) chest actor advances its open-animation state; pins which give-item site the live game runs."
topics = ["chest", "give-item", "field-VM 0x39", "randomizer", "keikoku"]

# ----------------------------------------------------------------
# Pre-scene-load capture for the keikoku chest room: standing on the
# kingdom overworld (map01), directly outside the keikoku (Ravine)
# entrance. Walking "up" for ~1s crosses the entrance portal, which
# streams the keikoku scene bundle (PROT entry 112) FRESH off the disc.
#
# This is the state an emulator-based randomizer check needs. A mednafen
# savestate captured INSIDE keikoku already has each chest's GIVE_ITEM
# (0x39) bytecode resident in RAM (keikoku_chest_pre's MAN sits at
# 0x8012f4c8), so loading it on a patched disc still grants the ORIGINAL
# item — the chest id is read from the resident RAM copy, never re-fetched
# from the patched disc. Captured on the overworld BEFORE the load, the
# patched MAN is read on scene entry, so a patched chest is actually
# observed. Companion to the deterministic engine oracle in
# crates/engine-core/tests/chest_randomizer_runtime_e2e.rs (which proves
# the same thing disc-free, by driving the patched MAN through the field VM).
# ----------------------------------------------------------------

[[scenarios]]
slot = 3
label = "keikoku_chest_preload"
ram_fingerprint_sha256 = "f697ac277c14720a179e542c24d4c50a249a226b3586ef38926a3dcf4e5abab4"
backup_fingerprint = "903185b20b9ada6fb6f18cc5abe5814caa090b7d8430635afdc92301d9dc8149"
phase = "field"
expected_active_scene = "map01"
description = "Kingdom overworld (map01), standing just outside the keikoku (Ravine) entrance. Walking up ~1s crosses the portal and streams the keikoku scene bundle fresh from disc — the pre-scene-load state for validating a patched-disc chest in an emulator without the savestate RAM-cache trap."
topics = ["chest", "randomizer", "keikoku", "scene-load", "preload", "map01"]

# ----------------------------------------------------------------
# PCSX-Redux trace state: standing directly in front of an early
# treasure chest (normally a Healing Leaf). Used to trace the field-VM
# GIVE_ITEM (op 0x39) path at runtime — capturing the RAM address the
# game reads the item-id operand from (s6 at 0x801E0450) and the granted
# id (FUN_800421D4 a0) — to verify whether a randomizer chest patch
# reaches the running game. Probe: autorun_chest_give_trace.lua.
# ----------------------------------------------------------------

[[scenarios]]
slot = 255
label = "chest_open_trace_pcsx"
backup_fingerprint = "083275b4ab53021546759c3a0d36534d43ffcce40c6dd7b1c0629414d84a07f4"
phase = "field"
pcsx_redux_sstate = "~/Tools/pcsx-redux/SCUS94254.sstate1"
description = "PCSX-Redux: standing in front of an early treasure chest (normally a Healing Leaf), for the field-VM GIVE_ITEM (0x39) runtime trace — operand RAM address + granted item id."
topics = ["chest", "give-item", "field-VM 0x39", "randomizer", "trace", "pcsx-redux"]

# ----------------------------------------------------------------
# Player-steal RAM capture (Evil God Icon equipped). The per-monster
# steal item is NOT a raw id in the PROT 867 monster record (exhaustive
# offset scan, docs/reference/open-rev-eng-threads.md), so closing where
# the steal item is read needs a live capture. Pair: Vahn attacks a
# skeleton and steals an "Incense"; `_pre` is the frame before the
# attack resolves, `_banner` is the "Stole the 'Incense'." message frame.
# Diff the pair to localize the RAM address carrying the stolen item id,
# then trace its source (a separate table/asset or a non-raw index).
# ----------------------------------------------------------------

[[scenarios]]
slot = 255
label = "player_steal_skeleton_pre"
ram_fingerprint_sha256 = "aca366d22fd9fca9127b1ce3a452e043c7a319dfea3b4d2de33668e9a4124dfc"
backup_fingerprint = "4dda3e8b2b9099024785896db03425a53c98bb9204ee910400802d4063612b8d"
phase = "battle"
expected_game_mode = 0x15
description = "Player-steal capture (Evil God Icon equipped): in battle, at the start of Vahn's attack that kills a skeleton and steals an 'Incense' — the frame before the steal resolves. Pairs with player_steal_skeleton_banner."
topics = ["steal", "Evil God Icon", "battle", "skeleton", "incense", "randomizer", "RE"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 255
label = "player_steal_skeleton_banner"
ram_fingerprint_sha256 = "f3225f2b7c9120e433f80a3a7852f56e00890983aa5ca587cba3515b9e656651"
backup_fingerprint = "1acdd38d71671c6d3eaa5e68999dd558d8d58ce61e8876526f010aeed6e7ed00"
phase = "battle"
expected_game_mode = 0x15
description = "Player-steal capture (Evil God Icon equipped): the 'Stole the `Incense`.' banner frame, after the steal from the skeleton resolved. Diff vs player_steal_skeleton_pre to localize the stolen-item-id RAM address."
topics = ["steal", "Evil God Icon", "battle", "skeleton", "incense", "randomizer", "RE"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

# ----------------------------------------------------------------
# Overworld battle-background capture set (4 camera angles). Vahn
# (solo, LV 1) vs a Gobu Gobu on the OVERWORLD continent, paused on
# the Begin/Run turn menu while the battle camera idly orbits the
# scene. The four slots are the SAME battle at four orbit angles, so
# diffing them isolates what is camera-dependent (the 3D stage:
# ground + mountains) from what is fixed (the sky backdrop).
#
# These pin the overworld battle backdrop = the map's scene_tmd_stream
# DOME (PROT 88 for map01): sky + mountain ring + grass ground,
# resident at RAM 0x800A8B34, registered as DAT_80076810. The ~738
# background prims (619 POLY_GT4 + 116 POLY_GT3) are the dome's
# gouraud-textured geometry. (NOT the world-map continent heightfield:
# the old "3715 hits in 0x80190000" was a prim-trace false positive
# from 3 degenerate clut=0 FT4 prims; the real-CLUT clusters match 0
# bytes in the continent windows.) Diffing the four angles isolates
# the orbit yaw `_DAT_8007b792` (the only camera global that changes:
# 224/3808/3136/2632) from the fixed rig (pitch 32, TR (0,1280,7680),
# H 256). Randomizer disc (mechanism is disc-independent). See
# docs/subsystems/battle.md "Battle background".

[[scenarios]]
slot = 2
label = "overworld_battle_bg_angle_a"
backup_fingerprint = "7269ae2b694a6a276037ad437eee9c331479c20d1eefb4f9c9a57ca982d46019"
phase = "battle"
expected_game_mode = 0x15
diff_against = [3, 4, 5]
description = "Overworld Vahn vs Gobu Gobu, Begin/Run turn menu, battle camera orbit angle A. Backdrop = the map01 scene_tmd_stream dome (PROT 88, resident at 0x800A8B34): sky + mountain ring + grass. Randomizer disc."
topics = ["battle", "battle background", "world map", "terrain", "camera orbit", "Gobu Gobu", "randomizer", "RE"]

[[scenarios]]
slot = 3
label = "overworld_battle_bg_angle_b"
backup_fingerprint = "32257e93ca63ebea0907c504b3d3e864fd13b97a430faa16010ff339ebadcde9"
phase = "battle"
expected_game_mode = 0x15
diff_against = [2, 4, 5]
description = "Overworld Vahn vs Gobu Gobu, Begin/Run turn menu, battle camera orbit angle B. Sister of overworld_battle_bg_angle_a at a different orbit angle. Randomizer disc."
topics = ["battle", "battle background", "world map", "terrain", "camera orbit", "Gobu Gobu", "randomizer", "RE"]

[[scenarios]]
slot = 4
label = "overworld_battle_bg_angle_c"
backup_fingerprint = "d0e4abc503bc0a914ae7989d8bb4c7ca28abbd95c95fe5114dc75bc732c74bf7"
phase = "battle"
expected_game_mode = 0x15
diff_against = [2, 3, 5]
description = "Overworld Vahn vs Gobu Gobu, Begin/Run turn menu, battle camera orbit angle C. Sister angle. Randomizer disc."
topics = ["battle", "battle background", "world map", "terrain", "camera orbit", "Gobu Gobu", "randomizer", "RE"]

[[scenarios]]
slot = 5
label = "overworld_battle_bg_angle_d"
backup_fingerprint = "af7a80a0d203fe57d904fdb1de559714b418100a08a38def0c5d09644357ebb9"
phase = "battle"
expected_game_mode = 0x15
diff_against = [2, 3, 4]
description = "Overworld Vahn vs Gobu Gobu, Begin/Run turn menu, battle camera orbit angle D. Sister angle. Randomizer disc."
topics = ["battle", "battle background", "world map", "terrain", "camera orbit", "Gobu Gobu", "randomizer", "RE"]

# ----------------------------------------------------------------------
# Minigame captures. Five of the game's side-activities, each parked in
# its playable state. Library-only (slot = 255): these were saved to
# ephemeral live mednafen slots, then content-hashed into saves/library/
# so they resolve durably via `backup_fingerprint` (the live slots get
# reused). The exact game_mode / scene per minigame is not yet pinned, so
# those optional fields are left off rather than asserted.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 255
label = "minigame_muscle_dome"
ram_fingerprint_sha256 = "e43e4df0d352417601d3b913665e5a711ca347886a60fe72d92bf535374932cc"
backup_fingerprint = "6917e1a452fc38f89016c876123950b7d0bbd2d98105e386a5d19a109170ad82"
phase = "minigame"
description = "Muscle Dome minigame (the rapid-button arm-wrestling / dome contest), parked in its playable state"
topics = ["minigame", "muscle dome", "RE"]

[[scenarios]]
slot = 255
label = "minigame_baka_fighter"
ram_fingerprint_sha256 = "d661866d1bd91a3e18593b30101952d2eb6c17c9f8be0096a63cbd010590d1a5"
backup_fingerprint = "c7a37c39178b2b1fa80b2fc8e150c83c97fddd98a897fe98d52172607e1bcf00"
phase = "minigame"
description = "Baka Fighter minigame (play as a battle-form party member; reuses the PROT 1204 battle character pack, see project_battle_char_pack_is_prot1204)"
topics = ["minigame", "baka fighter", "PROT 1204", "battle char pack", "RE"]

[[scenarios]]
slot = 255
label = "minigame_dance_noa"
ram_fingerprint_sha256 = "68994559d2663e3829bf06d5ccf6a41dde538c82944ddf16af28d386d1d830ee"
backup_fingerprint = "5214a97ff7cac6d12cfffcf2ba6474fd159639442eacea07ec6ada5c375948c1"
phase = "minigame"
description = "Noa dance-rhythm minigame, parked in its playable state"
topics = ["minigame", "dance", "rhythm", "Noa", "RE"]

[[scenarios]]
slot = 255
label = "minigame_fishing"
ram_fingerprint_sha256 = "14a5afcf9d040b64b3697468f2ba6dda4073ec0e5dc3db5de4b1a8ab2164c979"
backup_fingerprint = "9afc1f1cf6d26746765bc9a8dc46a0af7130deea9f1c68f9373a81ac10f97bf7"
phase = "minigame"
description = "Fishing minigame, parked in its playable state"
topics = ["minigame", "fishing", "RE"]

[[scenarios]]
slot = 255
label = "minigame_slot_machine"
ram_fingerprint_sha256 = "af9d9266c7712ddb5854a225237d153aaa170122446367557572e516a8df8b1f"
backup_fingerprint = "9dda14702d18b7d80ffcf1f7b4d6daefbbc6490d0ed60ee8c4aaae250351d3b8"
phase = "minigame"
description = "Casino slot-machine minigame, parked in its playable state (the coin economy the casino prize-exchange randomizer feeds)"
topics = ["minigame", "slot machine", "casino", "RE"]

[[scenarios]]
slot = 255
label = "party_basic_attack_vs_gobu_gobu"
backup_fingerprint = "658af41bc58b055801090f4fc91f7b4311b87aa9d505f49b94342e21bb0543f6"
phase = "battle"
expected_game_mode = 0x15
description = "Battle paused at the Begin/Reselect confirm with Vahn's queued basic Attack on a lone Gobu Gobu; pressing X (Begin) runs the turn (Gobu Gobu acts first, then Vahn's basic attack lands). Captures a party-member basic-Attack resolution for tracing the attack damage path (actor+0x1df move id, ctx+0x1014 held move-power record, damage dealt)."
topics = ["battle", "basic attack", "party damage", "move-power", "BattleMode", "RE"]

# ----------------------------------------------------------------------
# Pause-menu overlay captures (PROT 0899 resident at slot-A base
# 0x801CE818). Used to RAM-byte-match the menu overlay for the static
# overlay-extraction pipeline (docs/tooling/static-overlay-pipeline.md):
# the menu overlay replaces the field overlay (PROT 0897) in slot A, so
# the field VM at 0x801DE840 is NOT a prologue in these states while the
# equipment aggregator FUN_801CF650 at 0x801CF650 IS. Party = Vahn only.
#
# Two scene contexts (the menu overlay is identical in both): the field
# world-map scene `map01` (Drake Kingdom approach) and the town `town01`
# (Rim Elm). backup_fingerprint resolves the immutable library copy.
# ----------------------------------------------------------------------
[[scenarios]]
slot = 1
label = "menu_equipment_field"
backup_fingerprint = "9b0e7a6a4498c06e618d05fb1a1f9fa1d9d7d9101c5ed1e65d6175a6bcf24d98"
phase = "menu"
expected_active_scene = "map01"
# The menu-open captures all hold game_mode 0x17 (23, the CARD/menu-overlay
# per-frame mode): the in-field pause menu runs under the CARD mode pair,
# not field mode 3 (read directly off the six library saves at 0x8007B83C).
expected_game_mode = 0x17
description = "Equipment menu open from the field (map01, Drake Kingdom approach), party = Vahn. Menu overlay PROT 0899 resident at 0x801CE818 (field overlay 0897 swapped out). Source for the menu-overlay clean-copy RAM byte-match."
topics = ["menu", "equipment", "overlay PROT 0899", "static overlay", "map01"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 2
label = "menu_status_field"
backup_fingerprint = "904f3ae91fc5601f46403e93c88cea818db5d7e64345efbfa47e50c1096dab67"
phase = "menu"
expected_active_scene = "map01"
# The menu-open captures all hold game_mode 0x17 (23, the CARD/menu-overlay
# per-frame mode): the in-field pause menu runs under the CARD mode pair,
# not field mode 3 (read directly off the six library saves at 0x8007B83C).
expected_game_mode = 0x17
description = "Status menu open from the field (map01), party = Vahn. Menu overlay PROT 0899 resident at 0x801CE818."
topics = ["menu", "status", "overlay PROT 0899", "static overlay", "map01"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 3
label = "menu_options_field"
backup_fingerprint = "169eade7dfe4e646a39f99d855fa99da73f427747520a8572ddda57fcbb83378"
phase = "menu"
expected_active_scene = "map01"
# The menu-open captures all hold game_mode 0x17 (23, the CARD/menu-overlay
# per-frame mode): the in-field pause menu runs under the CARD mode pair,
# not field mode 3 (read directly off the six library saves at 0x8007B83C).
expected_game_mode = 0x17
description = "Options/config menu open from the field (map01), party = Vahn. Menu overlay PROT 0899 resident at 0x801CE818 (the options-menu config bundle boot.md attributes to PROT 0899)."
topics = ["menu", "options", "config", "overlay PROT 0899", "static overlay", "map01"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 6
label = "menu_equipment_town"
backup_fingerprint = "016b9175ec42d85514e0a7ad5fb0414c81a152d0f6701934713f08b869ed92c9"
phase = "menu"
expected_active_scene = "town01"
# The menu-open captures all hold game_mode 0x17 (23, the CARD/menu-overlay
# per-frame mode): the in-field pause menu runs under the CARD mode pair,
# not field mode 3 (read directly off the six library saves at 0x8007B83C).
expected_game_mode = 0x17
description = "Equipment menu open in town (town01, Rim Elm), party = Vahn. Menu overlay PROT 0899 resident at 0x801CE818. Town sibling of menu_equipment_field (the overlay is identical in town and field)."
topics = ["menu", "equipment", "overlay PROT 0899", "static overlay", "town01"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 7
label = "menu_status_town"
backup_fingerprint = "fd818b998623f3733ead6aa8337606efd25747c4fd4433cba0e427982c6f0784"
phase = "menu"
expected_active_scene = "town01"
# The menu-open captures all hold game_mode 0x17 (23, the CARD/menu-overlay
# per-frame mode): the in-field pause menu runs under the CARD mode pair,
# not field mode 3 (read directly off the six library saves at 0x8007B83C).
expected_game_mode = 0x17
description = "Status menu open in town (town01, Rim Elm), party = Vahn. Menu overlay PROT 0899 resident at 0x801CE818."
topics = ["menu", "status", "overlay PROT 0899", "static overlay", "town01"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 8
label = "menu_options_town"
backup_fingerprint = "e79b212a6284b03a3471a6cd4e69c0f39ac47d67b85452c764835ff6601c6eb3"
phase = "menu"
expected_active_scene = "town01"
# The menu-open captures all hold game_mode 0x17 (23, the CARD/menu-overlay
# per-frame mode): the in-field pause menu runs under the CARD mode pair,
# not field mode 3 (read directly off the six library saves at 0x8007B83C).
expected_game_mode = 0x17
description = "Options/config menu open in town (town01, Rim Elm), party = Vahn. Menu overlay PROT 0899 resident at 0x801CE818."
topics = ["menu", "options", "config", "overlay PROT 0899", "static overlay", "town01"]

[scenarios.overlay_slice]
start = 0x801C0000
end = 0x80200000

[[scenarios]]
slot = 255
label = "battle_noa_miracle_art_combo"
backup_fingerprint = "3cb1376abaf61f060c82c45aa1418cd82785ca8681d4586cbd4c52dd842a5d09"
phase = "battle"
expected_game_mode = 0x15
description = "Battle, Noa auto-executes a Miracle Art against an enemy on load (no input needed); she runs through the component arts over a few seconds before the find/replace finisher triggers. Capture point for the Super/Miracle Art action-queue builder (F-SUPER): the runtime emits the combo's interleaved queue + combo-specific connector bytes into the battle-action context at ctx[+0x274] (ctx = *(0x8007BD24)). Drives autorun_super_art_queue_builder.lua, which watches ctx+0x274/+0x276 for writes to pin the builder PC + the literal queue bytes."
topics = ["battle", "RE", "miracle art", "super art", "arts", "Noa", "ctx queue", "FUN_801E295C"]

[[scenarios]]
slot = 255
label = "battle_vahn_tri_somersault_super"
backup_fingerprint = "c2831f77f52da05ae709740853147f7ef49e8cd0e824b4650a98c3d1bf08b76c"
phase = "battle"
expected_game_mode = 0x15
description = "Vahn auto-executes the Tri-Somersault Super Art during a counterattack ~2s after load (no input). The byte-exact Super action queue is resident at actor[+0x1DF]: 0F 0E 19 27 0F 19 1F 0E 1A 2B 2B 2B = [Up Down][Starter Somersault 0x27 -> Up][Starter Cyclone 0x1F -> Down][SpecialStarter 0x1A][Tri-Somersault finisher 0x2B x3]. The tail matches super_art.rs's Tri-Somersault `replace` byte-exact, validating the combo-specific connectors (0x27->0F, 0x1F->0E). The dequeue site is pc 0x801D89D8 (consumes the queue head-first). Drives autorun_super_art_action_queue.lua."
topics = ["battle", "RE", "super art", "arts", "Vahn", "Tri-Somersault", "ctx queue", "FUN_801E295C"]

[[scenarios]]
slot = 255
label = "rimelm_wall_press_left"
ram_fingerprint_sha256 = "d91f8609639e75e8b02efb30af6809a87097e5ea91c65740022b9fa0ac0e942f"
backup_fingerprint = "1430efb5defb2552b61e4d75dfc5c73bc25a896e0663f0b7bcdbe4e29abf82b7"
phase = "field"
expected_active_scene = "town01"
description = "Rim Elm, player holding LEFT (-X) pressed against a wall mid-walk. The field-collision sub-cell-indexing discriminator: the wall byte that blocks the player must sit in the cell the retail derivation (zc=(z>>6)+2, xc=((x+0x3f)>>6)-1) reads at the leading-edge probes; resolves whether the engine's floor-derivation indexing of the walkability grid at *(_DAT_1F8003EC)+0x4000 is misaligned (FUN_801cfe4c / FUN_801d01b0)."
topics = ["field", "collision", "locomotion", "walkability grid", "FUN_801cfe4c", "RE"]

[[scenarios]]
slot = 255
label = "casino_ticket_counter"
backup_fingerprint = "68df0a5ca04ac334d07025fab777af85d95e0d9d8b3c5c62ed8dd2a9407cad2f"
phase = "field"
expected_game_mode = 0x03
description = "Casino floor, Vahn standing at the prize-exchange ticket counter (pre-interaction). Brackets the prize-exchange flow entry; pairs with casino_ticket_counter_dialog and casino_prize_shop for the prize-exchange game-mode + 0x3E sub-id hunt. Measured: game_mode 0x03 (field), field overlay PROT 0897 resident in slot A (clean prefix 1.0000)."
topics = ["casino", "prize exchange", "game mode", "RE"]

[[scenarios]]
slot = 255
label = "casino_ticket_counter_dialog"
backup_fingerprint = "9e4fdec5c2e4ec77c92c5b87948c1ac3f8a9ac4f86cedad9518223ff31c8c318"
phase = "field"
expected_game_mode = 0x03
description = "Casino floor, mid-dialog with the prize-exchange ticket-counter attendant. Middle of the prize-exchange flow bracket (see casino_ticket_counter). Measured: still game_mode 0x03 (field) — the dialog runs under the field overlay, not a menu session."
topics = ["casino", "prize exchange", "dialog", "game mode", "RE"]

[[scenarios]]
slot = 255
label = "casino_prize_shop"
backup_fingerprint = "231209cd8a981bf9b1d60d5410f2535546ba7546e240b28863be049376f70da4"
phase = "menu"
expected_game_mode = 0x17
description = "Inside the casino ticket-counter prize-exchange shop UI (the PROT 0899 @0x15D00 prize table's consumer). FINDING: the prize shop runs at game_mode 0x17 (the CARD/menu pair, same as the pause menu) with the MENU overlay PROT 0899 resident in slot A (clean prefix 1.0000) and the field overlay swapped out — the prize exchange is a menu-overlay session, consistent with its table + debit kernel living in PROT 0899."
topics = ["casino", "prize exchange", "shop", "PROT 0899", "game mode", "RE"]

[[scenarios]]
slot = 255
label = "baka_fighter_entry_pretransition"
backup_fingerprint = "1ac2c3d37e24ff428fa89db0dff01b2e402b21b1a29fba765f0d505ee36e0965"
phase = "field"
expected_game_mode = 0x03
description = "Starting the Baka Fighter minigame: pre-transition, Vahn taking a seat at the chair. Targets the minigame-entry window for the PROT 0896 (bat_back_dat) resident-identity hunt (statically recovered base 0x801C5818, believed the mode-24 OTHER overlay) and the 0x3E operand>=100 sub-id at 0x8007BA34. NEGATIVE: still game_mode 0x03, sub-id 0, and the 0896 unique head (base..0x801CE818) is neither at its base (0.0022 match) nor anywhere else in main RAM — the capture pre-dates the overlay load. The 100%-matching over-read tail is the resident FIELD overlay (0897) bytes sitting at exactly base+0x9000 = 0x801CE818, which independently corroborates the 0x801C5818 base. Next: autorun_minigame_overlay_capture.lua (mode-write triggered window dumps)."
topics = ["minigame", "baka fighter", "overlay PROT 0896", "game mode", "0x3E", "RE"]
