[meta]
schema = "j-replay-v1"
scenario = "v0_1_pre_battle_tetsu"
rng_seed = 3735929054
frames = 366

[[event]]
frame = 103
pad = 8192

[[event]]
frame = 110
pad = 0

# v0.1 Phase 1 (current): the engine drives a cold boot into Field/town01
# via BootSession::enter_field_live and stays there, converging against the
# field-phase retail snapshot (mc7, game_mode 0x03 -> Field). The Circle
# press at frame 103 is the seed for the deferred Battle leg.
[[expected]]
frame = 250
scene_mode = "Field"
active_scene = "town01"

# DEFERRED Battle leg (tracked, not yet asserted): reaching BattleMode here
# is the scripted Tetsu tutorial fight, which is gated on mc7's story state -
# a cold boot into town01 record 0 produces no Tetsu dialogue, and town
# scenes have a 0% random-encounter rate. It needs mc7's save state seeded
# into the clean-room World so the trigger is armed, then the field-VM
# dialogue-accept -> scripted-encounter install -> Field->Battle transition.
# When that lands, add:
#   [[expected]]
#   frame = 250
#   scene_mode = "Battle"
#   active_scene = "town01"
